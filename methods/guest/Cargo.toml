[package]
name = "face_verify"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = { version = "^3.0.5", features = ["std"] }
zkface-core = { path = "../../core" }
serde = { version = "1.0", features = ["derive"] }
